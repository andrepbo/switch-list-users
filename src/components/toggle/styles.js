import styled from "styled-components";

export const ToggleContainer = styled.div.attrs({
  className: "switch",
})`
  label {
    font-size: 1rem;
  }
`;

export const ToggleSpan = styled.span.attrs({
  className: "lever",
})``;
